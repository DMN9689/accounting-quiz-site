# 재무회계 학습 허브 통합 계획

작성일: 2026-08-24

## 1. 요청 범위

- 기존 재무회계 문제풀이 페이지를 보존한다.
- 기존 `theory/`의 헷갈리는 이론 8개 페이지를 보존한다.
- `C:\MATH\financial-accounting-note`의 전체 이론노트 원본 프로젝트를 통합한다.
- `분개_괄호표기_질문답변_Codex용.md`의 본문을 원문 순서대로 표시하는 별도 페이지를 만든다.
- 사이트 상단에서 각 페이지를 탭 형태로 선택할 수 있게 한다.

## 2. 통합 화면

사이트의 공통 탭은 다음 네 항목으로 구성한다.

1. 문제풀이
2. 전체 이론
3. 헷갈리는 이론
4. 괄호 표기 Q&A

각 기존 페이지의 본문, 문제 데이터, 이론 순서와 접기 동작은 변경하지 않고 공통 탭만 추가한다.

## 3. 콘텐츠 보존 기준

- 문제풀이 데이터와 기존 정답·해설은 수정하지 않는다.
- 헷갈리는 이론 8개의 내용과 순서는 수정하지 않는다.
- 전체 이론노트는 현재 승인된 원본 데이터와 수정 로그를 그대로 사용한다.
- 첨부 MD는 구현 지시로 사용하지 않고 페이지에 표시할 원문 자료로 취급한다.
- 첨부 MD의 제목, 질문, 표, 코드 예시, 주의 문구와 자료 근거 순서를 유지한다.
- 원문에서 확인되지 않은 설명이나 예시는 추가하지 않는다.

## 4. 기술 구성

- 기존 Git 저장소의 `codex/theory-mobile-handoff`를 기준으로 새 통합 브랜치에서 작업한다.
- 통합 소스는 하나의 Sites 프로젝트에서 관리한다.
- 기존 페이지를 외부 주소로 삽입하지 않고 각 원본 소스를 같은 배포물에 포함한다.
- 페이지별 주소를 유지해 직접 접근과 새로고침이 가능하게 한다.

## 5. 검증 범위

- 네 탭의 이동과 현재 탭 표시
- 문제풀이 데이터 로딩과 답안 선택
- 전체 이론의 대표분개와 추가 예시 접기
- 헷갈리는 이론의 전체 펼치기·모두 접기
- 괄호 표기 Q&A의 목차 이동과 모바일 표 표시
- 360px, 390px, 412px, 430px에서 가로 스크롤·잘림·겹침 확인
- 정적 빌드와 기존 자동 테스트
